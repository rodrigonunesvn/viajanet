'use strict';

describe('myApp.confirmacao module', function() {

  beforeEach(module('myApp.confirmacao'));

  describe('confirmacao controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var ConfirmacaoController = $controller('ConfirmacaoController');
      expect(ConfirmacaoController).toBeDefined();
    }));

  });
});