'use strict';

describe('myApp.landing module', function() {

  beforeEach(module('myApp.landing'));

  describe('landing controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var LandingController = $controller('LandingController');
      expect(LandingController).toBeDefined();
    }));

  });
});